---
name: update
project_root: "~/.dotfiles"
windows:
  - dotfiles:
    - cd ~/.dotfiles
    - git pull --rebase --autostash
  - vim:
    - vim +PlugInstall +PlugUpgrade +PlugUpdate +qall
  - hosts:
    - hosts update
  - brew:
    - brew upgrade
